// Compatibility endpoint retained for stale links; verification follows the
// same signed handoff and account-routing contract as current account access.
export {POST} from '@/app/api/applications/email-otp/verify/route';

export const runtime='nodejs';
