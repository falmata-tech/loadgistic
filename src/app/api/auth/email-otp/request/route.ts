// Compatibility endpoint: one implementation and one rate-limit namespace
// back the unified transporter account-access surface.
export {POST} from '@/app/api/applications/email-otp/request/route';

export const runtime='nodejs';
