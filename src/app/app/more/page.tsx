import { requireUser } from '@/lib/auth';
import { getBillingSummary } from '@/lib/repository.js';
import { PageHeader } from '@/components/page-header';
import { Flash } from '@/components/flash';
import { StatusPill } from '@/components/status-pill';
import { formatEtb } from '@/lib/domain.js';

export default async function MorePage({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const user=await requireUser();const query=await searchParams;const billing:any=getBillingSummary(user);return <div className="page"><PageHeader title="More" subtitle="Account, billing, support, verification, and cloud-readiness information."/><Flash error={query.error} success={query.success}/><div className="two-col"><div className="grid-3"><section className="card"><h3>Account</h3><p><strong>{user.name}</strong></p><p className="meta">{user.email}<br/>{user.role}</p></section><section className="card"><h3>Workspace</h3><p>{user.organization_name||user.provider_business_name||'Platform administration'}</p><p className="meta">Role and tenant boundaries are enforced in server services.</p></section><section className="card"><h3>Support</h3><p className="muted">The local MVP records operational activity and is ready for an email or support-tool adapter.</p></section><section className="card"><h3>Cloud migration</h3><p className="muted">Supabase PostgreSQL schema, RLS policies, and storage conventions are included.</p></section><section className="card"><h3>Browserbase</h3><p className="muted">Optional hosted Playwright smoke testing is prepared for AI-agent validation.</p></section><section className="card"><h3>Language</h3><p className="muted">The layout is ready for English, Amharic, and Afaan Oromo localization.</p></section></div><aside className="stack"><section className="card"><h3>Subscription</h3>{billing.subscription?<><p><strong>{billing.subscription.plan_name}</strong></p><StatusPill status={billing.subscription.status}/><div className="meta" style={{marginTop:8}}>{billing.subscription.billing_model.replaceAll('_',' ')}</div></>:<p className="muted">No plan assigned yet.</p>}</section>{billing.subscription?<section className="form-card"><h3>Submit payment proof</h3><p className="meta">Do not upload bank passwords, PINs, or OTP codes.</p><form action="/api/billing/payment-proof" method="post" encType="multipart/form-data" className="stack"><input name="amountEtb" type="number" min="1" required placeholder="Amount in ETB"/><input name="reference" placeholder="Bank or transfer reference"/><input name="file" type="file" accept="image/jpeg,image/png,image/webp,application/pdf"/><button className="button">Submit for review</button></form></section>:null}{billing.proofs?.length?<section className="card"><h3>Payment history</h3><div className="stack">{billing.proofs.map((p:any)=><div key={p.id}><strong>{formatEtb(p.amount_minor)}</strong> <StatusPill status={p.status}/><div className="meta">{new Date(p.submitted_at).toLocaleString()}</div></div>)}</div></section>:null}</aside></div></div>}
