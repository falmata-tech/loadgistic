import { requireUser } from '@/lib/auth';
import { listApplications, listAudit } from '@/lib/repository.js';
import { PageHeader } from '@/components/page-header';
import { Flash } from '@/components/flash';
import { StatusPill } from '@/components/status-pill';

export default async function ApplicationsPage({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const user=await requireUser(['ADMIN']);const query=await searchParams;const apps:any[]=listApplications(user);const audit:any[]=listAudit(user);return <div className="page"><PageHeader title="Business applications" subtitle="Approve, reject, or request more information. Every decision is audited."/><Flash error={query.error} success={query.success}/><div className="two-col"><section className="stack">{apps.map(app=><article className="card" key={app.id}><div style={{display:'flex',justifyContent:'space-between'}}><div><h3>{app.business_name}</h3><div className="meta">{app.application_type} · {app.email}</div></div><StatusPill status={app.status}/></div><p>{app.notes}</p><form action={`/api/admin/applications/${app.id}`} method="post" className="form-grid"><div className="form-group"><label>Decision</label><select name="status" defaultValue="APPROVED"><option value="APPROVED">Approve</option><option value="MORE_INFO">Request more information</option><option value="REJECTED">Reject</option></select></div><div className="form-group"><label>Review note</label><input name="notes" placeholder="Reason or next step"/></div><button className="button" type="submit">Save decision</button></form></article>)}</section><aside className="card"><h3>Recent audit activity</h3><div className="stack">{audit.slice(0,12).map(row=><div key={row.id}><strong>{row.action.replaceAll('_',' ')}</strong><div className="meta">{row.actor_name||'System'} · {row.entity_type} · {new Date(row.created_at).toLocaleString()}</div></div>)}</div></aside></div></div>}
